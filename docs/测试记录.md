# 测试记录

用于记录 PC、iPad、手机端显示效果、交互流程和动画稳定性测试。

## 2026-07-26 提交前审计

- [x] 使用项目 `node_modules/typescript/bin/tsc --noEmit` 完成 TypeScript 5.9.3 严格编译。
- [x] `git diff --check` 无空白错误。
- [x] 扫描 HTML、CSS、TypeScript 与皮肤 JSON 中的 `assets/` 引用，没有缺失文件。
- [x] 两套皮肤共 20 个天体配置素材全部存在。
- [x] 新增的卡通天体纹理均为 1774×887，写实球面纹理均为 2048×1024；土星环与星空素材格式、尺寸可读取。
- [x] 检查太阳系场景、彗星场景、地表投影、土星环前后遮挡、探访冻结、六入口互斥和相机屏幕命中关键路径。
- [x] 课程要求和成员3任务边界两份本地参考文档保持 Git 忽略状态，没有进入提交。
- [ ] 当前自动浏览器环境没有可用实例，合并前仍需在 PC、iPad 和手机浏览器完成视觉与手势验收。

## Canvas 公共动画骨架

### 当前静态检查

- [x] 公共运行时不激活场景时，不主动清空现有静态预览。
- [x] 动画控制器只维护一个待执行的 `requestAnimationFrame`。
- [x] 暂停和恢复会丢弃旧时间戳，避免首帧时间跳跃。
- [x] 帧间隔设置上限，避免浏览器后台恢复后出现大步长。
- [x] 主场景和专题图层都有退出、卸载与清理入口。
- [x] 相机状态和天体状态对外返回副本，避免跨模块直接篡改。
- [x] 现有播放、速度、缩放、重置和专题按钮事件已接入兼容桥。

### 已完成运行验证

- [x] 使用 TypeScript 5.9.3 严格模式编译，无类型错误。
- [x] 动画时钟、时间倍率和最大帧间隔检查通过。
- [x] 播放、暂停、继续和帧调度状态检查通过。
- [x] 速度级别到时间倍率映射检查通过。
- [x] 世界坐标与屏幕坐标往返转换检查通过。
- [x] 天体状态对外副本隔离检查通过。
- [x] 场景注册、切换、更新、渲染、退出和销毁检查通过。
- [x] 图层排序、启用、禁用和卸载检查通过。
- [x] 无界面核心检查共通过 33 项断言。

### 浏览器手动验证

测试日期：2026-07-25

测试方式：在本地开发页面打开浏览器 DevTools，通过公共运行时 API 和临时测试场景进行验证。

- [x] 可以通过 `getCanvasRuntime()` 获取唯一的 `CanvasRuntime` 实例。
- [x] 初始动画状态为 `idle`。
- [x] 调用 `start()` 后状态变为 `running`。
- [x] 调用 `pause()` 后状态变为 `paused`。
- [x] 调用 `resume()` 后状态恢复为 `running`。
- [x] 调用 `stop()` 后状态变为 `stopped`。
- [x] `setTimeScale(2)` 后统一时钟的时间倍率为 `2`。
- [x] 世界坐标 `(100, 50)` 转换为屏幕坐标后，可以正确还原为原世界坐标。
- [x] 坐标还原仅存在 JavaScript 浮点数精度范围内的微小误差。
- [x] `zoomBy()`、`panByScreen()`、`rotateBy()` 和 `reset()` 调用正常。
- [x] 临时动画场景可以注册、切换并持续绘制运动物体。
- [x] 调整时间倍率后，临时场景的运动速度同步变化。
- [x] `WorldStateStore` 可以写入、读取单个天体和读取全部天体状态。
- [x] 现有静态太阳系预览未被未激活场景的公共运行时覆盖。
- [x] 测试过程中未发现公共动画模块自身的运行时错误。


### 后续待验证

- [ ] ResizeObserver 下的画布尺寸和 DPR 更新。
- [ ] 播放、速度、缩放、重置和专题按钮实际交互。
- [ ] PC、iPad、手机浏览器实际运行。

## 模块1：太阳系总览场景

### 实现后的验证项目

- [ ] 页面启动后 `runtime.scenes.activeSceneId` 为 `solar-system`。
- [ ] `runtime.world` 中存在 `sun`、`moon` 和八大行星状态，不再包含 `pluto`。
- [ ] 点击播放后，地球和月球的 `orbitAngle` 持续变化。
- [ ] PC 和窄屏下所有行星中心始终落在对应的轨道线上。
- [ ] 水星公转快于地球，地球快于火星，外行星按真实周期比例明显更慢。
- [ ] `metadata.radiusEarths` 中水星、金星、地球、火星、木星、土星、天王星、海王星依次使用约 `0.383、0.949、1、0.532、10.973、9.14、3.981、3.865`。
- [ ] 总览视觉大小保持“木星 > 土星 > 天王星 ≈ 海王星 > 地球 ≈ 金星 > 火星 > 水星”的真实相对顺序。
- [ ] 轨道输入使用真实半长轴 AU；水星到火星集中在内太阳系，木星之后的轨道间距明显拉开。
- [ ] PC、iPad 和手机宽度下海王星轨道仍可通过默认视图或缩放查看，水星最小显示尺寸仍可点击。
- [ ] 卡通模式使用 `stars-milky-way-cartoon-small-stars.png`，背景中不再出现尺寸大于水星或火星的大型五角星、菱形星和十字星。
- [ ] 普通行星轨道保持细虚线，地球和月球强调轨道略粗但不压过天体；hover 后只适度增粗。
- [ ] 点击“轨道”按钮后，当前场景中的行星/月球或彗星轨迹及轨道标注全部隐藏；再次点击全部恢复。
- [ ] 太阳系与彗星场景中的轨迹全部显示为虚线，没有残留实线轨道。
- [ ] 轨道显示时，右下角图例使用不同颜色说明地球轨道和月球轨道。
- [ ] 鼠标悬停亮青色地球轨道时，轨道高亮并显示“地球轨道”浮动提示。
- [ ] 鼠标悬停亮黄色月球轨道时，轨道高亮并显示“月球轨道”浮动提示。
- [ ] 隐藏轨道后，右下角图例和轨道悬停提示同步消失。
- [ ] 地球的 `rotation` 和渲染器 `textureOffset` 持续变化，地表经纬纹理在可见半球上连续自转。
- [ ] 大陆接近球体左右边缘时逐渐压缩，不再像平面图片直接横向滑动。
- [ ] 地球纹理经过经度 0/1 接缝时没有空白、闪烁或明显跳变。
- [ ] 两种皮肤下地球的亮暗均随球面法线和太阳方向变化，昼夜边界连续。
- [ ] 聚焦并放大地球时离屏缓冲不再停留在 192×192，可根据显示尺寸和 DPR 提升到最高 512×512。
- [ ] 暂停后地球纹理停止，继续后从原位置恢复；调整速度后纹理自转速度同步变化。
- [ ] 写实模式太阳、月球和八大行星使用对应 `assets/skins/2k_*` 球面纹理。
- [ ] 卡通模式太阳、月球和八大行星使用对应 `assets/cartoon_skin/*-cartoon.png` 球面纹理，不再使用旧 SVG 平面天体。
- [ ] 两种模式都不显示黄色自转箭头；金星和天王星纹理自转方向与普通顺向行星相反。
- [ ] 写实太阳整体保持发光，不出现由太阳方向造成的黑暗半球，并保留外发光。
- [ ] 写实土星具有前后分层光环，球体遮挡后半光环，前半光环覆盖球体。
- [ ] 土星环读取 `2k_saturn_ring_alpha.png`，能看到素材中的明暗环带与透明缝隙，而不是原来的纯色程序化线条。
- [ ] 探访土星并上下、左右、斜向拖动时，球体与光环保持刚性连接，环的开合角和方向随 Arcball 连续变化。
- [ ] 两种皮肤探访土星时，从球体或光环范围内按下都能启动旋转；光环外按下不会误触发。
- [ ] 土星环转到接近正面时呈较圆的环，转到接近侧面时逐渐压成窄带，不发生突然翻转。
- [ ] 全方位旋转过程中，后半环始终被土星球体遮挡，前半环保持覆盖关系。
- [ ] 月球与各行星的亮面朝向太阳，背光面连续变暗。
- [ ] 水星、金星、火星、木星、土星、天王星和海王星靠近球体边缘的表面特征产生透视压缩。
- [ ] 月球位置围绕地球变化，且 `metadata.parentId` 为 `earth`。
- [ ] 世界坐标中月球与地球距离保持约 `30`，不受屏幕轨道倾角影响。
- [ ] 点击太阳、地球、月球或其他行星可以更新右侧信息面板。
- [ ] 点击太阳、月球或任一行星后，右侧均显示统一样式的基础百科和对应“探访”按钮。
- [ ] 卡通模式侧栏标题显示 `assets/svg` 对应天体图标，写实模式显示 `assets/images` 对应 PNG 图标，图标后紧跟中文名称。
- [ ] 普通状态标题显示“图标 + 天体名称”，探访状态显示“图标 + 正在探访天体名称”；换肤后图标立即切换且当前百科正文不丢失。
- [ ] 标题图标与文字同为 `1em` 并保持同行；土星等宽图标使用 `contain`，不被裁切或拉伸。
- [ ] 点击任一天体的“探访”后，相机在约 900ms 内平滑移动并放大到目标，不会瞬间跳转。
- [ ] 聚焦完成后目标直径约占左侧画布较短边的 55%，主体明显占据画面中央。
- [ ] 探访开始后只显示目标天体；其他天体、彗星和全部轨道均不出现。
- [ ] 探访期间所有天体的公转和自转状态保持不变，目标纹理不会自动旋转。
- [ ] 返回总览后其他天体从冻结位置继续运动，不发生轨道角或位置突然跳跃。
- [ ] 探访完成后，右侧显示目标天体专属的运动、重力、温度、成分和结构资料。
- [ ] 两种皮肤探访任一天体时，鼠标或单指拖动都可以改变球体上下、左右观察方向；停止拖动后球体保持当前方向。
- [ ] 从球面明显纹理处按下并拖动后，该纹理大致跟随指针移动，而不是仅按水平、垂直位移机械转轴。
- [ ] 只有从球体范围内按下才会启动 Arcball 旋转，球体外按下不会错误抓取目标天体。
- [ ] 探访时从背景区域拖动可以平移画面；平移后从球体新的屏幕位置左右或上下拖动仍能旋转球体。
- [ ] 从平移后的背景再次拖动不会意外退出探访状态，也不会把背景拖动识别为球体旋转。
- [ ] 斜向和绕边缘拖动可以产生自然的组合旋转，不出现 `yaw/pitch` 万向节锁。
- [ ] 写实天体全方位旋转时，光照方向保持稳定，不会跟着纹理错误转动；太阳始终使用自发光效果。
- [ ] 在卡通和写实模式之间切换后，当前探访目标、Arcball 朝向、冻结状态和相机位置保持连续。
- [ ] 点击“返回太阳系总览”后恢复探访前视角、基础百科和普通平移手势。
- [ ] 探访过程中若动画原本停止，会启动公共动画控制器并同步更新播放按钮状态。
- [ ] 调整速度后，公转和自转速度同步变化。
- [ ] 缩放、重置视角和换肤功能仍然可用。
- [ ] 切换写实模式后日志显示 `渲染器皮肤: realistic`，并加载 `assets/skins` 素材。
- [ ] 切回卡通模式后日志显示 `渲染器皮肤: cartoon`，并加载 `assets/cartoon_skin` 素材。
- [ ] 切换到未注册专题场景时，太阳系场景不会报错或创建第二个动画循环。

## 模块5：哈雷彗星轨道模拟

### 自动检查项目

- [x] TypeScript 5.9.3 严格模式编译通过。
- [x] 开普勒方程残差在允许误差内。
- [x] 0 秒和 48 秒的彗星位置一致，形成完整周期。
- [x] 近日点距离约为 0.59 AU，远日点距离约为 35.1 AU。
- [x] 近日点彗尾长度、宽度、亮度和活跃度均大于远日点。
- [x] 彗尾颜色会从远日点的冷蓝色变化为近日点的亮暖白色。
- [x] 模块5核心数学和彗尾参数共通过 18 项断言。

### 浏览器验收项目

- [ ] 点击“彗星”按钮后切换到 `comet` 场景；点击并列的“太阳系总览”入口后返回 `solar-system`。
- [ ] 点击播放后彗星沿虚线椭圆轨道持续运行。
- [ ] 彗星靠近近日点时明显加速，远离太阳后逐渐减速。
- [ ] 彗尾始终背离太阳，不随彗星运动方向错误翻转。
- [ ] 靠近太阳时彗尾逐渐拉长、变宽、变亮并改变颜色。
- [ ] 远离太阳后彗尾逐渐缩短、变暗并恢复冷蓝色。
- [ ] 速度滑块和加减速按钮可以同步加速或慢放轨道及彗尾变化。
- [ ] 暂停后彗星位置和彗尾状态保持不变，恢复后继续运行。
- [ ] “轨道”按钮可以隐藏和恢复彗星轨道、近日点及远日点标记。
- [ ] 点击彗星后信息面板显示当前距离、彗尾活跃度和轨道进度。
- [ ] PC、iPad 和手机尺寸下轨道、彗尾和状态卡片没有明显裁切。

## 模块6：交互操作与视角切换

### 自动检查项目

- [x] TypeScript 5.9.3 严格模式编译通过。
- [x] 单指和双指手势中心点计算正确。
- [x] 双指距离和旋转角计算正确。
- [x] 跨越 `-π/π` 时旋转增量可以正确归一化。
- [x] 相机聚焦优先使用场景的 `displayPosition`。
- [x] 没有显示坐标时会回退到逻辑世界坐标。
- [x] 三维轨道坐标保持轨道半径，非零轨道倾角会产生垂直分量。
- [x] 地心黄道方向可以按约 `23.44°` 黄赤交角转换为赤道坐标。
- [x] 赤经、赤纬可以结合当地恒星时和观察纬度转换为高度角、方位角。
- [x] 改变观察纬度会改变天体过中天时的高度。
- [x] 不同轨道倾角会产生不同赤纬和天空轨迹，不再统一压在同一路径。
- [x] 天体位于地平线以下时不显示。
- [x] 环顾角变化后，天空中的天体位置同步变化。
- [x] 抬头和低头时，地平线位置随观察俯仰角变化。
- [x] 世界坐标与屏幕坐标往返转换保持一致。
- [x] 锚点缩放前后，锚点对应的世界坐标保持不变。
- [x] 2.5D 地表观测专项检查通过 20 项断言。
- [x] 地表模式拖动保持第一视角，并修改观察者环顾角而不是平移世界相机。
- [x] 退出地表视角后，总览、聚焦和原有相机控制仍正常。
- [x] 模块6交互回归检查通过 15 项断言，本次两组检查共通过 35 项断言。
- [x] `git diff --check` 无空白错误。

### 浏览器和真机验收项目

- [ ] 总览中鼠标或单指拖动时，太阳系天体和轨道共同进行 Arcball 三维旋转，释放后不误选天体。
- [ ] 按住 `Shift` 后使用鼠标左键拖动，画面只平移且不会同时旋转太阳系。
- [ ] 滚轮缩放围绕当前指针位置进行。
- [ ] 底部不再显示独立“聚焦”按钮，“太阳系总览”按钮文案完整且可以恢复当前场景的完整视角。
- [ ] 星空背景使用 `assets/skins/2k_stars_milky_way.jpg`，放大、缩小和平移时与场景同步变化且不露出黑边。
- [ ] 进入“地表视角”后不再显示俯视太阳系，而是显示地平线、大气层和天空。
- [ ] 太阳、月球和可见行星随时间在天空中移动，且不同天体的高度和轨迹明显不同。
- [ ] 状态栏显示北纬 `35°`、当地恒星时和当前观察方向。
- [ ] 位于地平线以下的天体不会穿过地面显示。
- [ ] 在地表视角中拖动或使用方向键可以环顾天空，不会退出第一视角。
- [ ] 滚轮和双指缩放可以调整地表观察视野。
- [ ] 点击天空中的太阳、月球或行星可以更新信息面板。
- [ ] 彗星场景中禁止进入地表视角，并给出提示。
- [ ] 单指拖动不会触发页面滚动、文字选择或长按菜单。
- [ ] 双指只进行平移和缩放，不与太阳系 Arcball 旋转冲突，手势结束后不跳变。
- [ ] 手动拖动聚焦视角时会退出跟随，按钮和视角状态同步更新。
- [ ] 画布获得键盘焦点后，方向键、`+/-`、`0/E` 均正常工作。
- [ ] 设备旋转或容器尺寸变化后，Canvas CSS 尺寸、像素尺寸和 DPR 正确更新。
- [ ] PC、iPad、iPhone 和 Android 浏览器中控制栏可操作且不被安全区域遮挡。
