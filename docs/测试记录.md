# SolarKids 测试记录

本文件记录已经实际执行的测试和等待整合后执行的测试。未执行项目不会写成“通过”；失败用例修复后应保留原记录并增加复测记录。

## 2026-07-26 提交前审计

- [x] 使用项目 `node_modules/typescript/bin/tsc --noEmit` 完成严格编译。
- [x] `git diff --check` 无空白错误。
- [x] 扫描 HTML、CSS、TypeScript 与皮肤 JSON 中的 `assets/` 引用，没有缺失文件。
- [x] 两套皮肤共 20 个天体配置素材全部存在。
- [x] 成员1专题场景文件保持 `dev` 原实现，SVG/PNG 素材通过共享加载器的独立兼容缓存接入。
- [x] 新增的卡通天体纹理均为 1774×887，写实球面纹理均为 2048×1024；土星环与星空素材格式、尺寸可读取。
- [x] 检查太阳系场景、彗星场景、地表投影、土星环前后遮挡、探访冻结、六入口互斥和相机屏幕命中关键路径。
- [x] 课程要求和成员3任务边界两份本地参考文档保持 Git 忽略状态，没有进入提交。
- [ ] 当前自动浏览器环境没有可用实例，合并前仍需在 PC、iPad 和手机浏览器完成视觉与手势验收。

## Canvas 公共动画骨架

### 当前静态检查

- [x] 公共运行时不激活场景时，不主动清空现有静态预览。
- [x] 动画控制器只维护一个待执行的 `requestAnimationFrame`。
- [x] 暂停和恢复会丢弃旧时间戳，避免首帧时间跳跃。
- [x] 帧间隔设置上限，避免浏览器后台恢复后出现大步长。
- [x] 主场景和专题图层都有退出、卸载与清理入口。
- [x] 相机状态和天体状态对外返回副本，避免跨模块直接篡改。
- [x] 现有播放、速度、缩放、重置和专题按钮事件已接入兼容桥。

### 已完成运行验证

- [x] 使用项目锁文件安装的 TypeScript 严格编译，无类型错误。
- [x] 动画时钟、时间倍率和最大帧间隔检查通过。
- [x] 播放、暂停、继续和帧调度状态检查通过。
- [x] 速度级别到时间倍率映射检查通过。
- [x] 世界坐标与屏幕坐标往返转换检查通过。
- [x] 天体状态对外副本隔离检查通过。
- [x] 场景注册、切换、更新、渲染、退出和销毁检查通过。
- [x] 图层排序、启用、禁用和卸载检查通过。
- [x] 无界面核心检查共通过 33 项断言。

### 浏览器手动验证

测试日期：2026-07-25

测试方式：在本地开发页面打开浏览器 DevTools，通过公共运行时 API 和临时测试场景进行验证。

- [x] 可以通过 `getCanvasRuntime()` 获取唯一的 `CanvasRuntime` 实例。
- [x] 初始动画状态为 `idle`。
- [x] 调用 `start()` 后状态变为 `running`。
- [x] 调用 `pause()` 后状态变为 `paused`。
- [x] 调用 `resume()` 后状态恢复为 `running`。
- [x] 调用 `stop()` 后状态变为 `stopped`。
- [x] `setTimeScale(2)` 后统一时钟的时间倍率为 `2`。
- [x] 世界坐标 `(100, 50)` 转换为屏幕坐标后，可以正确还原为原世界坐标。
- [x] 坐标还原仅存在 JavaScript 浮点数精度范围内的微小误差。
- [x] `zoomBy()`、`panByScreen()`、`rotateBy()` 和 `reset()` 调用正常。
- [x] 临时动画场景可以注册、切换并持续绘制运动物体。
- [x] 调整时间倍率后，临时场景的运动速度同步变化。
- [x] `WorldStateStore` 可以写入、读取单个天体和读取全部天体状态。
- [x] 现有静态太阳系预览未被未激活场景的公共运行时覆盖。
- [x] 测试过程中未发现公共动画模块自身的运行时错误。


### 后续待验证

- [ ] ResizeObserver 下的画布尺寸和 DPR 更新。
- [ ] 播放、速度、缩放、重置和专题按钮实际交互。
- [ ] PC、iPad、手机浏览器实际运行。

## 二、本次测试环境

| 项目 | 内容 |
| --- | --- |
| 测试日期 | 2026-07-19 |
| 操作系统 | Windows |
| Node.js | v24.15.0 |
| npm | 11.12.1 |
| TypeScript | 7.0.2 |
| Vitest | 4.1.10 |
| 测试分支 | `feature/storage-game` |
| 自动测试命令 | `npm test -- --reporter=verbose` |
| 类型检查命令 | `npm run typecheck` |
| 构建命令 | `npm run build` |

除非单独说明，下表用例均使用上述日期和环境。自动测试使用内存 Storage 隔离数据，不依赖浏览器、UI 或 Canvas。

## 三、自动测试汇总

| 检查项 | 实际结果 | 结论 | 证据 |
| --- | --- | --- | --- |
| TypeScript 类型检查 | 进程退出码 0，无类型错误 | 通过 | `npm run typecheck` |
| 自动测试 | 3 个测试文件、39 项测试全部通过 | 通过 | `npm test -- --reporter=verbose` |
| TypeScript 构建 | 成功生成 `dist/`，包含语言 JSON、JS、声明文件和 source map | 通过 | `npm run build` |
| 依赖安全检查 | 49 个依赖，发现 0 个漏洞 | 通过 | `npm install` 输出 |
| Git 空白错误检查 | 退出码 0；仅提示 Windows 工作区换行转换，无空白错误 | 通过 | `git diff --check` |
| LocalStorage 访问边界 | `src/` 中只有 Storage 模块访问 `globalThis.localStorage` | 通过 | `rg -n "localStorage" src -g "*.ts"` |
| 小游戏依赖边界 | 未发现 DOM、HTML 元素或 Canvas API 引用 | 通过 | 对 `src/game/` 的静态检索 |

## 四、LocalStorage 模块测试

共同前置条件：使用 `UserStateStore` 和可注入的内存 Storage；测试文件为 `tests/storage.test.ts`。

| 编号 | 操作步骤 | 预期结果 | 实际结果 | 结论 | 证据 |
| --- | --- | --- | --- | --- | --- |
| S-01 | 创建空 Storage 并读取状态 | 返回完整默认状态 | 返回版本1、中文、默认皮肤、空进度和零游戏记录 | 通过 | 对应同名自动测试 |
| S-02 | 保存有效状态，再创建 Store 读取 | 内容与保存值一致 | 语言和皮肤等字段一致 | 通过 | 对应同名自动测试 |
| S-03 | 先保存皮肤，再切换英文 | 语言更新且皮肤不丢失 | 语言为 `en`，皮肤仍为 `night` | 通过 | 对应同名自动测试 |
| S-04 | 先保存英文，再切换皮肤 | 皮肤更新且语言不丢失 | 皮肤为 `colorful`，语言仍为 `en` | 通过 | 对应同名自动测试 |
| S-05 | 两次记录同一行星，第二次带空格 | 行星 ID 规范化且不重复 | 只保存一个 `earth` | 通过 | 对应同名自动测试 |
| S-06 | 保存80分成功记录 | 次数、成功数、最好分和最近分正确 | 结果为1、1、80、80 | 通过 | 对应同名自动测试 |
| S-07 | 先保存90分，再保存40分 | 最好分保持90 | 最好分90，最近分40 | 通过 | 对应同名自动测试 |
| S-08 | 写入损坏 JSON 后读取 | 回退默认状态且不抛异常 | 返回完整默认状态 | 通过 | 对应同名自动测试 |
| S-09 | 写入缺少字段的数据后读取 | 自动补齐默认字段 | 已保留英文和火星访问记录，其余字段补齐 | 通过 | 对应同名自动测试 |
| S-10 | 写入非法语言 `fr` | 回退中文 | 返回 `zh-CN` | 通过 | 对应同名自动测试 |
| S-11 | 分别保存负数、`NaN` 和无限分数 | 规范化为安全值 | 最近分均为0 | 通过 | 对应同名自动测试 |
| S-12 | 保存设置后重置 | 删除存储并返回默认状态 | 存储键删除，再次读取为默认 | 通过 | 对应同名自动测试 |
| S-13 | 模拟 Storage 读取抛错 | 应用不崩溃并返回默认状态 | 未抛错，返回默认状态 | 通过 | 对应同名自动测试 |
| S-14 | 模拟 Storage 写入抛错后再次读取 | 应用不崩溃，并在当前会话保留状态 | 未抛错，再次读取仍为目标语言 | 通过 | 对应同名自动测试 |

补充测试：

| 场景 | 实际结果 | 结论 |
| --- | --- | --- |
| 重复完成同一学习任务 | 任务 ID 只保存一次 | 通过 |
| 读取不支持的数据版本 | 整体回退到版本1默认状态 | 通过 |

## 五、多语言模块测试

共同前置条件：词典文件为 `src/data/language.json`，语言服务为 `I18nService`；测试文件为 `tests/language.test.ts`。

| 编号 | 操作步骤 | 预期结果 | 实际结果 | 结论 | 证据 |
| --- | --- | --- | --- | --- | --- |
| L-01 | 使用标准 `JSON.parse` 读取词典 | 无解析错误 | 解析成功 | 通过 | 对应同名自动测试 |
| L-02 | 排序比较中英文键 | 键集合完全相同 | 完全相同 | 通过 | 对应同名自动测试 |
| L-03 | 遍历所有词典值 | 均为非空字符串 | 未发现空值或非字符串 | 通过 | 对应同名自动测试 |
| L-04 | 首次读取开始按钮 | 默认返回中文 | 返回“开始探索” | 通过 | 对应同名自动测试 |
| L-05 | 切换英文后读取开始按钮 | 返回英文 | 返回“Start Exploring” | 通过 | 对应同名自动测试 |
| L-06 | 查询英文缺失键和完全不存在的键 | 先回退中文，再回退键名 | 两级回退均符合约定 | 通过 | 对应同名自动测试 |
| L-07 | 保存英文后重建语言服务 | 语言仍为英文 | 返回 `en` | 通过 | 对应同名自动测试 |
| L-08 | 传入非法语言代码 | 回退中文 | 返回 `zh-CN` 和中文文案 | 通过 | 对应同名自动测试 |
| L-09 | 检查八大行星的名称、3个关键词和2条知识 | 中英文全部完整 | 96个必需字段均存在 | 通过 | 对应同名自动测试 |
| L-10 | 逐条检查儿童英文，并自动检查长度和严厉错误词 | 使用短句、常用词，无严厉提示 | 16条英文知识均不超过100字符，内容检查无明显难词堆叠 | 通过 | 自动测试及词典内容检查 |

## 六、小游戏纯逻辑测试

共同前置条件：小游戏是拖动距离与轨道变化关系的教学近似，不是精确引力仿真；测试文件为 `tests/orbit-game.test.ts`。

| 编号 | 操作步骤 | 预期结果 | 实际结果 | 结论 | 证据 |
| --- | --- | --- | --- | --- | --- |
| G-01 | 创建地球游戏状态 | 坐标等于初始值，分数有效 | 偏移0、稳定、0分、未完成 | 通过 | 对应同名自动测试 |
| G-02 | 拖动距离为0 | 等级为 `stable` | 返回 `stable` | 通过 | 对应同名自动测试 |
| G-03 | 拖动10单位 | 等级为 `small` | 小变化、50分、未完成 | 通过 | 对应同名自动测试 |
| G-04 | 拖动60单位 | 等级为 `large` | 大变化、100分、已完成 | 通过 | 对应同名自动测试 |
| G-05 | 测试5和60两个阈值两侧 | 边界归类符合常量定义 | 5为稳定，60为大变化，中间为小变化 | 通过 | 对应同名自动测试 |
| G-06 | 连续拖动两次 | 始终相对初始位置计算 | 第二次偏移正确为30 | 通过 | 对应同名自动测试 |
| G-07 | 大幅拖动后重置 | 所有状态恢复初始值 | 坐标、等级、分数、完成状态均恢复 | 通过 | 对应同名自动测试 |
| G-08 | 输入 `NaN` 或无限坐标 | 明确拒绝且不产生 `NaN` 状态 | 抛出 `TypeError` | 通过 | 对应同名自动测试 |
| G-09 | 结束一个大变化游戏 | 返回完整结果 | 行星、偏移、等级、分数和成功状态完整 | 通过 | 对应同名自动测试 |
| G-10 | 将完成结果写入 Storage | 游戏记录正确更新 | 次数1、成功1、最好100、最近100 | 通过 | 对应同名自动测试 |
| G-11 | 保存旧状态后执行拖动 | 不修改输入状态 | 旧状态保持不变，新状态对象独立 | 通过 | 对应同名自动测试 |

补充测试：

| 场景 | 实际结果 | 结论 |
| --- | --- | --- |
| 空行星 ID、负偏移和 `NaN` 偏移 | 均抛出 `TypeError` | 通过 |
| 相同状态与坐标重复计算 | 两次结果完全一致 | 通过 |

## 七、待整合测试

以下项目依赖成员2或成员3的代码，当前未执行，不能标记为通过：

| 编号 | 测试项目 | 当前状态 | 依赖 |
| --- | --- | --- | --- |
| I-01 | HTML、CSS、JavaScript、TypeScript 的最终使用检查 | 待整合 | 完整项目 |
| I-02 | Canvas 太阳系动画 | 待整合 | 成员3 |
| I-03 | SVG 元素显示 | 待整合 | 成员2 |
| I-04 | PC、iPad、手机响应式布局 | 待整合 | 成员2及完整页面 |
| I-05 | 播放、暂停、缩放和视角切换 | 待整合 | 成员2、3 |
| I-06 | 最终 UI 全部文案切换 | 待整合 | 成员2 |
| I-07 | 最终皮肤切换和刷新保持 | 待整合 | 成员2 |
| I-08 | Canvas 行星拖动、轨道反馈和重置 | 待整合 | 成员3 |
| I-09 | 核心流程均不超过5步 | 待整合 | 完整页面 |
| I-10 | 动画平滑且不过度闪烁 | 待整合 | 成员2、3 |
| I-11 | 四名成员 AI 开发记录完整 | 待整合 | 全员 |
| I-12 | 稳定版本合并到 `main` | 待整合 | 全员 |
| I-13 | 最终 zip 解压并正常运行 | 待整合 | 稳定 `main` |

## 八、本次结论

截至 2026-07-19，成员4可独立完成的 Storage、多语言和小游戏纯逻辑模块已经通过类型检查、39项自动测试和 TypeScript 构建。UI、Canvas、多设备显示和最终 zip 测试保留为待整合项目。

## 九、2026-07-26 本地整合验证

整合基线：`origin/dev` 的 UI、Canvas、日月食和磁场成果，与 `feature/storage-game` 的 Storage、多语言和小游戏纯逻辑成果。

| 检查项 | 实际结果 | 结论 |
| --- | --- | --- |
| TypeScript 类型检查 | UI、Canvas、Storage、多语言和游戏代码无类型错误 | 通过 |
| 自动测试 | 3个测试文件、39项测试全部通过 | 通过 |
| 页面构建 | 成功生成浏览器使用的 `src/**/*.js` 和 source map | 通过 |
| 桌面浏览器加载 | 1440×900 下 Canvas、贴图、侧栏和控制栏正常显示 | 通过 |
| 手机浏览器加载 | 390×844 下主场景和侧栏正常显示 | 部分通过 |
| 静态资源请求 | 页面、脚本、CSS、皮肤JSON和天体素材均返回成功 | 通过 |
| 缺失资源 | `favicon.ico` 返回404，不影响主功能 | 待补充 |
| 手机控制栏 | 底部按钮超出可视宽度，需要横向滚动或重新排布 | 待成员2修复 |

本次整合只统一现有成果，没有把语言、Storage和小游戏接入 `src/main.ts`，也没有实现太阳风场景。这些内容进入成员4下一阶段开发。

## 十、2026-07-26 成员4产品接入验收

测试分支：`integration/member4-latest`

### 10.1 自动检查

| 检查项 | 实际结果 | 结论 |
| --- | --- | --- |
| TypeScript 类型检查 | `npm run typecheck` 退出码0 | 通过 |
| 自动测试 | 4个测试文件、51项测试 | 全部通过 |
| 生产构建 | `npm run build` 成功输出 `dist/` | 通过 |
| 中英文词典 | 中英文各194键，无缺失键 | 通过 |
| 场景纯逻辑 | 粒子、跨设备坐标和结算门槛共7项 | 通过 |
| 词典请求失败回退 | 英文状态下回退为中文核心操作文案 | 通过 |
| 浏览器 ESM | 所有相对模块使用 `.js` 扩展名，请求成功 | 通过 |

### 10.2 桌面浏览器验收

视口：1440×900；Canvas 实际 CSS 尺寸：1102×743。

| 操作 | 实际结果 | 结论 |
| --- | --- | --- |
| 打开太阳风 | `aria-pressed=true`，侧栏显示太阳风实验室 | 通过 |
| 切换三种模式 | 三种模式截图哈希不同，粒子路线和说明变化 | 通过 |
| 切换英文 | 标题、功能按钮、侧栏和场景文字立即变为英文 | 通过 |
| 打开日月食和磁场 | Canvas 标题、工具栏、标签和说明均为英文 | 通过 |
| 打开小游戏 | 侧栏和 Canvas 显示 Orbit Explorer | 通过 |
| 拖动地球 | 轨道变化、100分、成功状态显示 | 通过 |
| 检查游戏记录 | `playCount=1`、`successCount=1`、`bestScore=100` | 通过 |
| 切换写实皮肤 | 当前皮肤变为 `realistic` | 通过 |
| 刷新页面 | 英文、写实皮肤、游戏记录和任务均保持 | 通过 |
| 运行时异常 | Chrome Runtime exception 数量为0 | 通过 |
| 播放太阳风 | 相隔350ms的 Canvas 哈希不同 | 通过 |
| 暂停太阳风 | 相隔450ms的 Canvas 哈希完全一致 | 通过 |
| 本轮网络请求 | 未捕获4xx或5xx响应 | 通过 |

### 10.3 平板与手机验收

| 视口 | 实际结果 | 结论 |
| --- | --- | --- |
| 768×1024 | Canvas 768×663，控制栏无横向溢出，全部按钮可见 | 通过 |
| 390×844 | Canvas 390×500，控制栏390×92，两行显示，全部按钮可见 | 通过 |
| 390×844太阳风 | 标题、三模式、太阳、地球、粒子和磁层说明无关键遮挡 | 通过 |
| 390×844小游戏 | 原轨道、太阳、地球、分数、提示和重置按钮无关键遮挡 | 通过 |
| 390×844触摸拖动 | Chrome原生触摸输入完成拖动并得到100分 | 通过 |
| 轻触或小变化后松开 | 不创建游戏记录，仍可继续拖动 | 通过 |
| 未重置再次触摸拖动 | `playCount` 保持1，不重复结算 | 通过 |
| 触摸重置后再次拖动 | `playCount` 变为2，可开始新一局 | 通过 |

移动端修复前，控制栏 `scrollWidth=1012`，太阳风入口起始于661px，
小游戏入口起始于764px。修复后 `scrollWidth=390`，14个按钮均在视口内。

### 10.4 本轮发现的集成缺陷

浏览器首次接入时发现无扩展名导入导致4个404：

```text
/dist/game/orbit-game
/dist/i18n/i18n
/dist/storage/defaults
/dist/storage/storage
```

补齐 `.js` 后，上述请求均返回成功，应用初始化和按钮事件恢复。该问题不会被
TypeScript 类型检查或 Node 单元测试发现，因此保留浏览器原生 ESM 验收为发布前必测项。

### 10.5 仍需人工验收

- [ ] 成员4本人在真实手机或 iPad 上复核已通过模拟触摸的拖动流程。
- [ ] 成员4本人复核太阳风科学表述。
- [ ] 成员4本人复核儿童中英文文案。
- [ ] 团队完成模块1、5、6后再执行最终全项目验收。
- [ ] 稳定 `main` 打包后在新目录完成 zip 解压运行。

## 模块1：太阳系总览场景

### 实现后的验证项目

- [ ] 页面启动后 `runtime.scenes.activeSceneId` 为 `solar-system`。
- [ ] `runtime.world` 中存在 `sun`、`moon` 和八大行星状态，不再包含 `pluto`。
- [ ] 点击播放后，地球和月球的 `orbitAngle` 持续变化。
- [ ] PC 和窄屏下所有行星中心始终落在对应的轨道线上。
- [ ] 水星公转快于地球，地球快于火星，外行星按真实周期比例明显更慢。
- [ ] `metadata.radiusEarths` 中水星、金星、地球、火星、木星、土星、天王星、海王星依次使用约 `0.383、0.949、1、0.532、10.973、9.14、3.981、3.865`。
- [ ] 总览视觉大小保持“木星 > 土星 > 天王星 ≈ 海王星 > 地球 ≈ 金星 > 火星 > 水星”的真实相对顺序。
- [ ] 轨道输入使用真实半长轴 AU；水星到火星集中在内太阳系，木星之后的轨道间距明显拉开。
- [ ] PC、iPad 和手机宽度下海王星轨道仍可通过默认视图或缩放查看，水星最小显示尺寸仍可点击。
- [ ] 卡通模式使用 `stars-milky-way-cartoon-small-stars.png`，背景中不再出现尺寸大于水星或火星的大型五角星、菱形星和十字星。
- [ ] 普通行星轨道保持细虚线，地球和月球强调轨道略粗但不压过天体；hover 后只适度增粗。
- [ ] 点击“轨道”按钮后，当前场景中的行星/月球或彗星轨迹及轨道标注全部隐藏；再次点击全部恢复。
- [ ] 太阳系与彗星场景中的轨迹全部显示为虚线，没有残留实线轨道。
- [ ] 轨道显示时，右下角图例使用不同颜色说明地球轨道和月球轨道。
- [ ] 鼠标悬停亮青色地球轨道时，轨道高亮并显示“地球轨道”浮动提示。
- [ ] 鼠标悬停亮黄色月球轨道时，轨道高亮并显示“月球轨道”浮动提示。
- [ ] 隐藏轨道后，右下角图例和轨道悬停提示同步消失。
- [ ] 地球的 `rotation` 和渲染器 `textureOffset` 持续变化，地表经纬纹理在可见半球上连续自转。
- [ ] 大陆接近球体左右边缘时逐渐压缩，不再像平面图片直接横向滑动。
- [ ] 地球纹理经过经度 0/1 接缝时没有空白、闪烁或明显跳变。
- [ ] 两种皮肤下地球的亮暗均随球面法线和太阳方向变化，昼夜边界连续。
- [ ] 聚焦并放大地球时离屏缓冲不再停留在 192×192，可根据显示尺寸和 DPR 提升到最高 512×512。
- [ ] 暂停后地球纹理停止，继续后从原位置恢复；调整速度后纹理自转速度同步变化。
- [ ] 写实模式太阳、月球和八大行星使用对应 `assets/skins/2k_*` 球面纹理。
- [ ] 卡通模式太阳、月球和八大行星使用对应 `assets/cartoon_skin/*-cartoon.png` 球面纹理，不再使用旧 SVG 平面天体。
- [ ] 两种模式都不显示黄色自转箭头；金星和天王星纹理自转方向与普通顺向行星相反。
- [ ] 写实太阳整体保持发光，不出现由太阳方向造成的黑暗半球，并保留外发光。
- [ ] 写实土星具有前后分层光环，球体遮挡后半光环，前半光环覆盖球体。
- [ ] 土星环读取 `2k_saturn_ring_alpha.png`，能看到素材中的明暗环带与透明缝隙，而不是原来的纯色程序化线条。
- [ ] 探访土星并上下、左右、斜向拖动时，球体与光环保持刚性连接，环的开合角和方向随 Arcball 连续变化。
- [ ] 两种皮肤探访土星时，从球体或光环范围内按下都能启动旋转；光环外按下不会误触发。
- [ ] 土星环转到接近正面时呈较圆的环，转到接近侧面时逐渐压成窄带，不发生突然翻转。
- [ ] 全方位旋转过程中，后半环始终被土星球体遮挡，前半环保持覆盖关系。
- [ ] 月球与各行星的亮面朝向太阳，背光面连续变暗。
- [ ] 水星、金星、火星、木星、土星、天王星和海王星靠近球体边缘的表面特征产生透视压缩。
- [ ] 月球位置围绕地球变化，且 `metadata.parentId` 为 `earth`。
- [ ] 世界坐标中月球与地球距离保持约 `30`，不受屏幕轨道倾角影响。
- [ ] 点击太阳、地球、月球或其他行星可以更新右侧信息面板。
- [ ] 点击太阳、月球或任一行星后，右侧均显示统一样式的基础百科和对应“探访”按钮。
- [ ] 卡通模式侧栏标题显示 `assets/svg` 对应天体图标，写实模式显示 `assets/images` 对应 PNG 图标，图标后紧跟中文名称。
- [ ] 普通状态标题显示“图标 + 天体名称”，探访状态显示“图标 + 正在探访天体名称”；换肤后图标立即切换且当前百科正文不丢失。
- [ ] 标题图标与文字同为 `1em` 并保持同行；土星等宽图标使用 `contain`，不被裁切或拉伸。
- [ ] 点击任一天体的“探访”后，相机在约 900ms 内平滑移动并放大到目标，不会瞬间跳转。
- [ ] 聚焦完成后目标直径约占左侧画布较短边的 55%，主体明显占据画面中央。
- [ ] 探访开始后只显示目标天体；其他天体、彗星和全部轨道均不出现。
- [ ] 探访期间所有天体的公转和自转状态保持不变，目标纹理不会自动旋转。
- [ ] 返回总览后其他天体从冻结位置继续运动，不发生轨道角或位置突然跳跃。
- [ ] 探访完成后，右侧显示目标天体专属的运动、重力、温度、成分和结构资料。
- [ ] 两种皮肤探访任一天体时，鼠标或单指拖动都可以改变球体上下、左右观察方向；停止拖动后球体保持当前方向。
- [ ] 从球面明显纹理处按下并拖动后，该纹理大致跟随指针移动，而不是仅按水平、垂直位移机械转轴。
- [ ] 只有从球体范围内按下才会启动 Arcball 旋转，球体外按下不会错误抓取目标天体。
- [ ] 探访时从背景区域拖动可以平移画面；平移后从球体新的屏幕位置左右或上下拖动仍能旋转球体。
- [ ] 从平移后的背景再次拖动不会意外退出探访状态，也不会把背景拖动识别为球体旋转。
- [ ] 斜向和绕边缘拖动可以产生自然的组合旋转，不出现 `yaw/pitch` 万向节锁。
- [ ] 写实天体全方位旋转时，光照方向保持稳定，不会跟着纹理错误转动；太阳始终使用自发光效果。
- [ ] 在卡通和写实模式之间切换后，当前探访目标、Arcball 朝向、冻结状态和相机位置保持连续。
- [ ] 点击“返回太阳系总览”后恢复探访前视角、基础百科和普通平移手势。
- [ ] 探访过程中若动画原本停止，会启动公共动画控制器并同步更新播放按钮状态。
- [ ] 调整速度后，公转和自转速度同步变化。
- [ ] 缩放、重置视角和换肤功能仍然可用。
- [ ] 切换写实模式后日志显示 `渲染器皮肤: realistic`，并加载 `assets/skins` 素材。
- [ ] 切回卡通模式后日志显示 `渲染器皮肤: cartoon`，并加载 `assets/cartoon_skin` 素材。
- [ ] 日食/月食、彗星和磁场入口可在同一公共运行时中互斥切换，不会创建第二个动画循环。
- [ ] 日食/月食和磁场场景在卡通模式使用原有 SVG 球体，在写实模式使用原有 PNG 球体。
- [x] 点击太阳雨入口后切换到 `solar-rain`，显示太阳风三种教学模式且无重复入口。

## 模块5：哈雷彗星轨道模拟

### 自动检查项目

- [x] 项目锁文件安装的 TypeScript 严格模式编译通过。
- [x] 开普勒方程残差在允许误差内。
- [x] 0 秒和 48 秒的彗星位置一致，形成完整周期。
- [x] 近日点距离约为 0.59 AU，远日点距离约为 35.1 AU。
- [x] 近日点彗尾长度、宽度、亮度和活跃度均大于远日点。
- [x] 彗尾颜色会从远日点的冷蓝色变化为近日点的亮暖白色。
- [x] 模块5核心数学和彗尾参数共通过 18 项断言。

### 浏览器验收项目

- [ ] 点击“彗星”按钮后切换到 `comet` 场景；点击并列的“太阳系总览”入口后返回 `solar-system`。
- [ ] 点击播放后彗星沿虚线椭圆轨道持续运行。
- [ ] 彗星靠近近日点时明显加速，远离太阳后逐渐减速。
- [ ] 彗尾始终背离太阳，不随彗星运动方向错误翻转。
- [ ] 靠近太阳时彗尾逐渐拉长、变宽、变亮并改变颜色。
- [ ] 远离太阳后彗尾逐渐缩短、变暗并恢复冷蓝色。
- [ ] 速度滑块和加减速按钮可以同步加速或慢放轨道及彗尾变化。
- [ ] 暂停后彗星位置和彗尾状态保持不变，恢复后继续运行。
- [ ] “轨道”按钮可以隐藏和恢复彗星轨道、近日点及远日点标记。
- [ ] 点击彗星后信息面板显示当前距离、彗尾活跃度和轨道进度。
- [ ] PC、iPad 和手机尺寸下轨道、彗尾和状态卡片没有明显裁切。

## 模块6：交互操作与视角切换

### 自动检查项目

- [x] 项目锁文件安装的 TypeScript 严格模式编译通过。
- [x] 单指和双指手势中心点计算正确。
- [x] 双指距离和旋转角计算正确。
- [x] 跨越 `-π/π` 时旋转增量可以正确归一化。
- [x] 相机聚焦优先使用场景的 `displayPosition`。
- [x] 没有显示坐标时会回退到逻辑世界坐标。
- [x] 三维轨道坐标保持轨道半径，非零轨道倾角会产生垂直分量。
- [x] 地心黄道方向可以按约 `23.44°` 黄赤交角转换为赤道坐标。
- [x] 赤经、赤纬可以结合当地恒星时和观察纬度转换为高度角、方位角。
- [x] 改变观察纬度会改变天体过中天时的高度。
- [x] 不同轨道倾角会产生不同赤纬和天空轨迹，不再统一压在同一路径。
- [x] 天体位于地平线以下时不显示。
- [x] 环顾角变化后，天空中的天体位置同步变化。
- [x] 抬头和低头时，地平线位置随观察俯仰角变化。
- [x] 世界坐标与屏幕坐标往返转换保持一致。
- [x] 锚点缩放前后，锚点对应的世界坐标保持不变。
- [x] 2.5D 地表观测专项检查通过 20 项断言。
- [x] 地表模式拖动保持第一视角，并修改观察者环顾角而不是平移世界相机。
- [x] 退出地表视角后，总览、聚焦和原有相机控制仍正常。
- [x] 模块6交互回归检查通过 15 项断言，本次两组检查共通过 35 项断言。
- [x] `git diff --check` 无空白错误。

### 浏览器和真机验收项目

- [ ] 总览中鼠标或单指拖动时，太阳系天体和轨道共同进行 Arcball 三维旋转，释放后不误选天体。
- [ ] 按住 `Shift` 后使用鼠标左键拖动，画面只平移且不会同时旋转太阳系。
- [ ] 滚轮缩放围绕当前指针位置进行。
- [ ] 底部不再显示独立“聚焦”按钮，“太阳系总览”按钮文案完整且可以恢复当前场景的完整视角。
- [ ] 星空背景使用 `assets/skins/2k_stars_milky_way.jpg`，放大、缩小和平移时与场景同步变化且不露出黑边。
- [ ] 进入“地表视角”后不再显示俯视太阳系，而是显示地平线、大气层和天空。
- [ ] 太阳、月球和可见行星随时间在天空中移动，且不同天体的高度和轨迹明显不同。
- [ ] 状态栏显示北纬 `35°`、当地恒星时和当前观察方向。
- [ ] 位于地平线以下的天体不会穿过地面显示。
- [ ] 在地表视角中拖动或使用方向键可以环顾天空，不会退出第一视角。
- [ ] 滚轮和双指缩放可以调整地表观察视野。
- [ ] 点击天空中的太阳、月球或行星可以更新信息面板。
- [ ] 彗星场景中禁止进入地表视角，并给出提示。
- [ ] 单指拖动不会触发页面滚动、文字选择或长按菜单。
- [ ] 双指只进行平移和缩放，不与太阳系 Arcball 旋转冲突，手势结束后不跳变。
- [ ] 手动拖动聚焦视角时会退出跟随，按钮和视角状态同步更新。
- [ ] 画布获得键盘焦点后，方向键、`+/-`、`0/E` 均正常工作。
- [ ] 设备旋转或容器尺寸变化后，Canvas CSS 尺寸、像素尺寸和 DPR 正确更新。
- [ ] PC、iPad、iPhone 和 Android 浏览器中控制栏可操作且不被安全区域遮挡。

## 2026-07-27 最新 dev 合并与太阳雨入口验收

合并基线：`origin/dev@2019867` 与
`integration/member4-latest@dac688b`。太阳雨入口冲突按最新 `dev` 的
`btn-solar-rain`、`solarkids:showSolarRain`、`solar-rain` 协议处理。

### 自动检查

| 检查项 | 实际结果 | 结论 |
| --- | --- | --- |
| TypeScript 类型检查 | `npm run typecheck` 退出码0 | 通过 |
| 自动测试 | 4个测试文件、52项测试全部通过 | 通过 |
| 入口协议回归 | `SOLAR_RAIN_SCENE_ID === 'solar-rain'` | 通过 |
| 生产构建 | `npm run build` 成功输出 `dist/` | 通过 |
| 冲突与空白检查 | 无冲突标记；`git diff --check` 无空白错误 | 通过 |

### Chrome 桌面验收

视口：1440×900；Canvas 实际 CSS 尺寸：1120×839。

| 操作 | 实际结果 | 结论 |
| --- | --- | --- |
| 初始加载 | `solar-system` 总览显示，Canvas 像素采样非空 | 通过 |
| 入口唯一性 | 1个太阳雨、1个小游戏入口，旧太阳风按钮不存在 | 通过 |
| 打开太阳雨 | 按钮选中，侧栏显示太阳风实验室 | 通过 |
| 播放太阳雨 | 500ms 前后 Canvas 哈希不同 | 通过 |
| 暂停太阳雨 | 500ms 前后 Canvas 哈希相同 | 通过 |
| 切换彗星 | 按钮选中，侧栏显示哈雷彗星 | 通过 |
| 返回总览 | 总览按钮选中，侧栏恢复地球 | 通过 |
| 打开小游戏 | 按钮选中，侧栏显示轨道探索小游戏 | 通过 |
| 切换英文和写实皮肤 | 页面、入口、侧栏切换并写入 LocalStorage | 通过 |
| 刷新保持 | 英文和 `realistic` 皮肤保持 | 通过 |
| 运行时和资源错误 | 0个异常，0个失败请求 | 通过 |

专题场景中的总览状态条已隐藏，避免压住 Canvas 标题；返回
`solar-system` 后状态条恢复。

### Chrome 移动端验收

| 视口 | 实际结果 | 结论 |
| --- | --- | --- |
| 390×844 | Canvas 390×454，控制栏390×137 | 通过 |
| 390×844控制栏 | 17个按钮均在视口内，触控尺寸不小于40×40 | 通过 |
| 390×844太阳雨 | 标题、三模式、粒子、磁层和侧栏无关键遮挡 | 通过 |
| 390×844小游戏 | 轨道、天体、提示和侧栏无关键遮挡 | 通过 |
| 320×700 | 控制栏 `clientWidth=scrollWidth=320` | 通过 |
| 窄屏速度控制 | 隐藏滑块但保留减速、加速按钮 | 通过 |
| 移动端 Toast | 位于控制栏上方，不遮挡按钮 | 通过 |
| 运行时和资源错误 | 0个异常，0个失败请求 | 通过 |

仍需成员4本人使用真实手机或 iPad 复核触摸手感、太阳风科学表述和
儿童中英文文案。
