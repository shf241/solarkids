# SolarKids 成员4开发说明与验收记录

负责人：杨丰瑞

## 一、职责与边界

### 1.1 职责来源

成员4原始职责：

1. TypeScript 项目结构和成员4模块整理。
2. 使用 LocalStorage 保存语言、皮肤、学习进度和小游戏记录。
3. 实现中文与儿童科普英文切换。
4. 实现拖动行星观察轨道变化的小游戏。
5. 参与整合、测试、AI说明书和最终压缩包验收。
6. 实现模块4太阳风动态演示。
7. 把已经完成的小游戏接入团队UI和公共Canvas运行时。

### 1.2 不属于成员4的核心实现

成员4不接管以下模块：

- 模块1太阳系自转、公转和球面渲染。
- 模块2日食与月食。
- 模块3太阳和地球磁场。
- 模块5哈雷彗星。
- 模块6公共相机、第一视角和场景切换。
- 模块7整体视觉设计。

成员4可以为整合修复共享入口、类型映射、多语言属性和响应式冲突，但不把
这些模块的业务成果写成成员4独立实现。语音讲解和画中画的负责人仍需全组
确认。

## 二、产品设计与用户分析

### 2.1 目标用户

主要用户是5至12岁儿童：

- 注意力保持时间较短，需要快速反馈。
- 更容易理解可拖动、可切换和持续运动的视觉内容。
- 对颜色、图形和明确的选中状态比较敏感。
- 不适合阅读长段落或复杂参数。

辅助用户是教师和家长：

- 需要稳定、可重复的演示流程。
- 需要暂停、调速、切换视角和恢复默认状态。
- 需要科学表述清楚，不能把教学近似写成真实物理仿真。

### 2.2 成员4相关用户问题

| 用户问题 | 对应设计 |
| --- | --- |
| 刷新后设置丢失 | 使用LocalStorage统一保存语言和皮肤 |
| 学习过程没有反馈 | 记录已访问行星和已完成任务 |
| 英文界面不适合儿童 | 使用短句、稳定术语和中英文同键词典 |
| 太阳风难以理解 | 拆成向外辐射、磁层屏障、极区互动三种模式 |
| 小游戏在不同设备难度不一致 | 按视口短边归一化拖动距离 |
| 轻触就被判定完成 | 只有达到大变化并松开后才结算 |
| 移动端按钮过多 | 小屏图标化并分三排，保持40像素触控尺寸 |

### 2.3 操作步骤控制

成员4两个主要流程均不超过5步：

太阳雨入口下的太阳风演示：

1. 点击“太阳雨”。
2. 选择向外辐射、磁层屏障或极区互动。
3. 使用播放、暂停或速度控制观察变化。

轨道小游戏：

1. 点击“轨道小游戏”。
2. 按住地球拖动。
3. 松开查看变化等级和得分。
4. 点击重置开始下一次尝试。

## 三、关键设计决策

| 问题 | 最终选择 | 原因 |
| --- | --- | --- |
| Storage结构 | 单一版本化根键 | 便于整体读取、升级、回退和清空 |
| 天体与任务标识 | 稳定英文ID | 不受显示语言变化影响 |
| Storage不可用 | 保留会话内状态 | 浏览器禁用存储时主功能仍可运行 |
| 词典加载失败 | 回退内置中文核心词典 | 避免页面初始化失败 |
| 英文缺失键 | 英文回退中文，再回退键名 | 不显示`undefined` |
| 小游戏模型 | 拖动距离教学近似 | 可理解、可测试，不冒充真实引力计算 |
| 小游戏结算 | 仅大变化、松开后、每局一次 | 防止轻触、小拖动和重复事件污染记录 |
| 太阳风模型 | 概念粒子动画 | 解释传播和偏转，不伪造实时空间天气数据 |
| 动画调度 | 使用团队公共Canvas运行时 | 避免第二个`requestAnimationFrame` |
| 太阳风入口协议 | 服从最新`dev`的“太阳雨”协议 | 避免重复按钮和事件冲突 |
| 美术素材 | 复用团队已有天体素材 | 保持风格统一并减少版权风险 |
| 构建输出 | 使用`dist/` | 避免生成JS污染`src/`并被测试误加载 |

太阳风科学表达边界：

- 向外辐射表示太阳向行星空间释放带电粒子。
- 磁层屏障表示多数粒子被地球磁场改变路线。
- 极区互动表示少量粒子可能沿磁力线进入极区。
- 动画不提供真实速度、密度或空间天气预报。

小游戏科学表达边界：

- 得分来自拖动距离和教学阈值。
- 轨道形状是变化反馈，不是数值积分结果。
- 页面不得宣称它是精确引力模拟。

## 四、当前实现与技术接口

### 4.1 代码交付物

```text
src/
├─ storage/
│  ├─ defaults.ts
│  ├─ storage.ts
│  ├─ types.ts
│  └─ index.ts
├─ i18n/
│  ├─ i18n.ts
│  ├─ types.ts
│  └─ index.ts
├─ game/
│  ├─ orbit-game.ts
│  ├─ types.ts
│  └─ index.ts
├─ integration/
│  ├─ member4Integration.ts
│  └─ index.ts
├─ data/
│  └─ language.json
└─ canvas/scenes/
   ├─ member4SceneLogic.ts
   ├─ orbitGameScene.ts
   ├─ registerMember4Scenes.ts
   └─ solarWindScene.ts

tests/
├─ storage.test.ts
├─ language.test.ts
├─ orbit-game.test.ts
└─ member4-scenes.test.ts
```

### 4.2 LocalStorage约定

唯一根键：

```text
solarkids.user-state.v1
```

当前状态结构：

```ts
interface SolarKidsUserState {
  version: 1;
  language: 'zh-CN' | 'en';
  skinId: string;
  learningProgress: {
    visitedPlanetIds: string[];
    completedTaskIds: string[];
  };
  gameRecord: {
    playCount: number;
    successCount: number;
    bestScore: number;
    lastScore: number;
  };
}
```

默认值：

- 语言：`zh-CN`
- 皮肤：`cartoon`
- 学习记录：空数组
- 游戏次数和分数：0

公开接口位于`src/storage/index.ts`：

- `loadUserState()`、`saveUserState()`
- `updateUserState()`、`resetUserState()`
- `setLanguage()`、`setSkin()`
- `markPlanetVisited()`、`markTaskCompleted()`
- `saveGameResult()`
- `UserStateStore`

Storage模块负责：

- 补全旧数据缺失字段。
- 拒绝不支持的数据版本。
- 规范化语言、分数、天体ID和任务ID。
- 数组去重。
- 捕获读写异常并保持应用可用。

UI和Canvas模块不得自行解析LocalStorage JSON，应调用上述接口。

### 4.3 多语言约定

词典文件为`src/data/language.json`，当前中英文各215个同名键。

加载和回退流程：

1. 浏览器异步加载JSON。
2. 校验`zh-CN`和`en`均为非空字符串字典。
3. 校验两个字典的键完全一致。
4. 加载失败时使用内置中文核心词典。
5. 当前语言缺键时先回退中文，再显示键名。

主要接口：

- `loadTranslationData()`
- `I18nService.translate()`
- `I18nService.hasTranslation()`
- `createMember4Integration()`
- `applyDocumentLanguage()`
- `localizeInfo()`

HTML使用以下属性接入：

- `data-i18n`
- `data-i18n-title`
- `data-i18n-aria-label`

稳定ID、数据字段和DOM事件不翻译，只翻译用户可见文字和无障碍标签。

### 4.4 应用接入

`src/main.ts`当前完成：

1. 启动时创建`Member4Integration`。
2. 从Storage恢复语言和皮肤。
3. 预加载对应皮肤并同步Canvas与侧栏图标。
4. 绑定语言切换并即时重绘侧栏、Canvas提示和无障碍标签。
5. 监听天体选择并记录已访问ID。
6. 注册成员4两个Canvas场景。
7. 场景退出和页面卸载时清理监听器。

皮肤ID使用：

- `cartoon`
- `realistic`

成员1专题场景仍使用`svg | png`素材类型，`main.ts`负责从皮肤ID映射，不修改
成员1场景协议。

### 4.5 公共场景协议

成员4场景由`registerMember4Scenes()`统一注册：

| 用户入口 | 按钮ID | DOM事件 | 场景ID |
| --- | --- | --- | --- |
| 太阳雨 | `btn-solar-rain` | `solarkids:showSolarRain` | `solar-rain` |
| 轨道小游戏 | `btn-orbit-game` | `solarkids:showOrbitGame` | `orbit-game` |

`solar-rain`是成员3在最新`dev`中确定的公共协议。成员4把太阳风教学动画接到
该场景，不再注册旧的`solar-wind`场景，不再增加`btn-solar-wind`按钮。

两个场景均遵守`CanvasScene`生命周期：

- `enter()`绑定自身事件。
- `render()`使用公共Canvas绘制。
- `reset()`恢复场景状态。
- `exit()`移除指针和键盘监听器。
- 不创建独立动画循环。

### 4.6 太阳风场景

场景包含三种模式：

| 模式 | 教学内容 |
| --- | --- |
| `radial` | 粒子从太阳向外传播 |
| `shield` | 多数粒子在地球磁层前方偏转 |
| `aurora` | 少量粒子沿磁力线进入极区 |

粒子状态更新使用公共`frame.deltaMs`，因此自动受播放、暂停和速度倍率控制。
太阳和地球贴图使用团队当前皮肤素材。

### 4.7 轨道小游戏

纯逻辑阈值：

| 归一化拖动距离 | 等级 | 分数 | 是否完成 |
| --- | --- | --- | --- |
| `0`至`5` | `stable` | 0 | 否 |
| 大于`5`且小于`60` | `small` | 50 | 否 |
| 大于等于`60` | `large` | 100 | 是 |

Canvas场景将不同设备的拖动距离归一化到500单位范围，避免同一比例手势在PC和
手机得到不同结果。

结算规则：

1. 只有`large`状态才算完成。
2. 只有松开或取消指针时检查结算。
3. 每次重置前最多保存一次。
4. 成功后记录任务`orbit-game-large-change`。
5. 轻触和小变化不增加`playCount`。

## 五、AI协同开发过程

### 5.1 时间线

| 日期 | 输入与目标 | AI辅助内容 | 结果 |
| --- | --- | --- | --- |
| 2026-07-18至19 | 先完成不依赖他人的部分 | 计划、Storage、多语言、小游戏纯逻辑和测试 | 39项测试通过 |
| 2026-07-26 | 审计全分支并统一本地版本 | 拉取分支、比较提交、合并有效成果、分析接口 | 建立`integration/member4-latest` |
| 2026-07-26 | 完成新增职责 | 接入Storage和语言，实现太阳风、小游戏场景 | 51项测试及浏览器验收通过 |
| 2026-07-27 | 合并最新`dev` | 处理新总览、彗星、皮肤和太阳雨入口冲突 | 52项测试及多设备验收通过 |
| 2026-07-27 | 统一成员4文档 | 消除计划、接口和验收文件重复 | 本文档成为唯一成员4文档 |

### 5.2 AI生成或辅助内容

AI辅助完成：

- 成员4任务拆解和可验收计划。
- TypeScript与Vitest配置。
- Storage类型、默认值、读写、规范化和异常回退。
- 中英文JSON、加载校验和查询逻辑。
- 自动测试、浏览器验收和冲突分析。
- 本文档结构与内容整理。

### 5.3 成员4提供的独立判断

成员4明确提出并作为开发约束：

- 小游戏状态模型、计分、重置和保存逻辑。
- 太阳风粒子与三种教学模式。
- 轨道小游戏Canvas交互。
- 主入口接线、皮肤映射和学习进度事件。

### 5.4 AI建议中未直接采用的内容

| AI建议或尝试 | 处理 | 原因 |
| --- | --- | --- |
| 合并所有远端分支 | 未采用 | 部分旧分支无新增业务且落后于`dev` |
| 安装额外`serve`依赖 | 撤回 | 没有必要增加高风险间接依赖 |
| 直接使用屏幕像素作为游戏距离 | 未采用 | PC和手机难度不一致 |
| 继续使用`default`皮肤ID | 未采用 | 团队实际ID为`cartoon`和`realistic` |
| 下载网络太阳风插图 | 未采用 | 现有素材足够，且可减少版权和风格问题 |
| 保留`solar-wind`入口 | 未采用 | 与最新`dev`的太阳雨协议冲突 |

### 5.5 学生修改记录真实性

目前有明确记录的是成员4提出的需求、边界和整合决策。尚未形成逐文件的本人
代码修改记录，因此不能把AI辅助修改写成学生逐行修改。

成员4本人复核后，应在第十节填写真实修改，包括修改文件、修改内容、理由和
复测结果。

## 六、用户体验优化过程

开发和验收中发现并修正：

| 问题 | 发现方式 | 修正 |
| --- | --- | --- |
| 默认皮肤ID与UI不一致 | 审计`skins.json` | 默认值改为`cartoon` |
| 浏览器ESM导入404 | Chrome资源检查 | 相对导入补`.js` |
| 手机控制栏横向溢出 | 390宽度DOM测量 | 图标化并调整为三排 |
| 320宽度仍有2像素溢出 | 窄屏复测 | 移除入口组额外水平内边距 |
| Toast遮挡移动端按钮 | 截图检查 | 移到控制栏上方 |
| 专题标题被总览状态条遮挡 | 桌面截图检查 | 状态条仅在`solar-system`显示 |
| 轻触小游戏产生记录 | 触摸流程检查 | 仅完成大变化后结算 |
| 重复触摸重复结算 | LocalStorage检查 | 每次重置前只保存一次 |
| Canvas语言标签未同步 | 整合检查 | 切换语言时同步状态和ARIA标签 |

移动端保持的交互标准：

- 所有按钮落在视口内。
- 按钮触控尺寸不小于40×40像素。
- 320宽度下无横向滚动。
- 极窄屏隐藏速度滑块，但保留减速和加速按钮。

## 七、测试与验收证据

### 7.1 自动检查

最新执行结果：

| 检查项 | 命令 | 结果 |
| --- | --- | --- |
| TypeScript类型检查 | `npm run typecheck` | 通过 |
| 自动测试 | `npm test -- --reporter=verbose` | 4个文件、52项通过 |
| 生产构建 | `npm run build` | 通过 |
| Git空白检查 | `git diff --check` | 通过 |
| 中英文键一致性 | `tests/language.test.ts` | 215键对齐 |
| 太阳雨协议 | `tests/member4-scenes.test.ts` | 场景ID为`solar-rain` |

测试分布：

| 文件 | 数量 | 覆盖内容 |
| --- | --- | --- |
| `tests/storage.test.ts` | 17 | 默认值、读写、异常、规范化、记录 |
| `tests/language.test.ts` | 14 | JSON、键一致、切换、回退、儿童英文 |
| `tests/orbit-game.test.ts` | 13 | 阈值、拖动、重置、保存、纯函数 |
| `tests/member4-scenes.test.ts` | 8 | 场景协议、粒子、坐标归一化、结算门槛 |

### 7.2 桌面Chrome验收

视口：1440×900。

已验证：

- 初始太阳系总览Canvas非空。
- 页面只有一个太阳雨入口和一个小游戏入口。
- 旧`btn-solar-wind`不存在。
- 太阳雨按钮切换到太阳风实验室。
- 播放时Canvas像素哈希变化，暂停后保持不变。
- 彗星、太阳系总览和小游戏可以互斥切换。
- 中文、英文和写实皮肤可以切换。
- 刷新后语言和皮肤保持。
- 运行时异常为0。
- 失败资源请求为0。

### 7.3 移动端Chrome验收

| 视口 | 验收结果 |
| --- | --- |
| 390×844 | Canvas、侧栏和137像素高控制栏顺序正常 |
| 390×844 | 17个按钮全部在视口内，尺寸不小于40×40 |
| 390×844 | 太阳风标题、模式、粒子和磁层无关键遮挡 |
| 390×844 | 小游戏轨道、天体、提示和侧栏无关键遮挡 |
| 320×700 | `clientWidth`与`scrollWidth`均为320 |
| 320×700 | Toast位于控制栏上方 |

上述为Chrome设备尺寸模拟和原生触摸事件验收，不替代成员4本人在真实手机或
iPad上的最终复核。