export {
  calculateOrbitObservation,
  calculateOrbitZone,
  createGameState,
  finishGame,
  INNER_ORBIT_MAX,
  MAX_ORBIT_RADIUS_RATIO,
  MIN_ORBIT_RADIUS_RATIO,
  OUTER_ORBIT_MIN,
  resetGame,
  saveOrbitGameResult,
  setOrbitRadius,
} from "./orbit-game.js";
export type {
  ExploredOrbitZones,
  GameResult,
  OrbitGameState,
  OrbitObservation,
  OrbitZone,
} from "./types.js";
